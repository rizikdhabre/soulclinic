# Firebase-first OTP with eligible Twilio fallback (no development-code bypass).
OTP_DEV_CODE=
# Generate independent server-only secrets of at least 32 random bytes locally.
OTP_SOURCE_HASH_SECRET=
CUSTOMER_SESSION_SECRET=

# Optional Redis read cache (disabled by default)
REDIS_CACHE_ENABLED=false
UPSTASH_REDIS_REST_KV_REST_API_URL=
UPSTASH_REDIS_REST_KV_REST_API_TOKEN=
